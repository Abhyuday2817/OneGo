from django.apps import AppConfig


class MentorshipReviewsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.mentorship_reviews"  # Make sure this matches the app's directory